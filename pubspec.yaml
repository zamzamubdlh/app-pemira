name: pemira_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  google_fonts: ^6.1.0
  d_view: ^0.0.5
  d_input: ^0.1.2
  d_info: ^0.2.0
  input_form_field: ^0.0.5

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/onboarding/
